name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check secrets exist
        run: |
          [ -n "${{ secrets.FTP_SERVER }}" ] && echo "FTP_SERVER ok" || (echo "FTP_SERVER missing" && exit 1)
          [ -n "${{ secrets.FTP_USERNAME }}" ] && echo "FTP_USERNAME ok" || (echo "FTP_USERNAME missing" && exit 1)
          [ -n "${{ secrets.FTP_PASSWORD }}" ] && echo "FTP_PASSWORD ok" || (echo "FTP_PASSWORD missing" && exit 1)

      - name: Deploy website to Hostinger
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          port: 21
          local-dir: ./
          server-dir: /public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            .github/**
            .vscode/**
            package.json
            package-lock.json
            tailwind.config.js
            scraping.py
            results.json
            scratch_main.js